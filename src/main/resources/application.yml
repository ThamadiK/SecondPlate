server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eventapp?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: eventapp
    password: eventapp
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
